<section>
    <div class="container mt-5">
        <div class="row">
            <table class="table table-hover">
        <thead class="table-light">
            <tr>
                <th scope="col">Item</th>
                <th scope="col">Title</th>
                <th scope="col">Quantity</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            {{#each products}}
            <tr>{{#each this.product}}
                <td> <img src="/Productimages/{{this._id}}.jpg" alt="image" style="width:50px;height:50px"></td>
                <td>{{this.name}}</td> {{/each}}
                <td>
                     <button class="btn btn-primary mr-3" onclick="reduceCart('{{this._id}}')">-</button> 
                      <span>{{this.quantity}}</span>
                       <button class="btn btn-primary ml-3" onclick="increaseCart('{{this._id}}')">+</button>
                </td>
                <td>
                     <a href="#" class="btn btn-danger" onclick="return confirm('are you sure you want to delete {{this.name}}?')">Delete</a>
                </td>
            </tr>
           {{/each}}
        </tbody>
    </table>
        </div>
    </div>    
</section>
